import 'package:vtparse/vtparse.dart';
import 'package:test/test.dart';

void main() {
  group('A group of tests', () {
    // TODO: Write tests.
    // Somehow force the VTStateMachine into every state and pass each state
    // every possible value (0x00 -> 0x7F). Make sure that every state has a
    // proper transition?
  });
}
